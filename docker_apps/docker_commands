# Build image
docker build -t mysql-image .

# Run image
docker run -d -p 3306:3306 --name mysql_container mysql-image
# Run with a volume
docker run -d \
  -p 8080:80 \
  -v "D:\The Abd\Pro\Cours\cloud-course\docker_apps\nginex_webapp\website:/usr/share/nginx/html:ro" \
  nginx:abd

# Enter to the container
docker exec -it <container_name_or_id> /bin/bash